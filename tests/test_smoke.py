"""Placeholder test so the suite is green from the scaffolding onward.

Real tests live alongside each feature (signature, idempotency, downloader, client).
"""


def test_smoke() -> None:
    assert True
